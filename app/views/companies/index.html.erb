<h1>Listing companies</h1>

<table>
  <tr>
  </tr>

<% @companies.each do |companies| %>
  <tr>
    <td><%= link_to 'Show', companies %></td>
    <td><%= link_to 'Edit', edit_companies_path(companies) %></td>
    <td><%= link_to 'Destroy', companies, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New companies', new_companies_path %>